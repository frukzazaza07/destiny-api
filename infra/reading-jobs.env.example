# Example values only. Supply separate production credentials through deployment configuration.
RABBITMQ_PASSWORD=example-only-replace-before-deploy
READING_WORKER_KEY=example-only-worker-key-replace-before-deploy
TAROT_LLM_CLOUD_ENDPOINT=https://provider.example.test/v1/chat/completions
TAROT_LLM_CLOUD_API_KEY=example-only-replace-before-deploy
TAROT_LLM_CLOUD_MODEL=configured-cloud-model
ALLOW_CLOUD_RAW_QUESTION=false
CLOUD_MAX_CONCURRENCY=4
CLOUD_REQUESTS_PER_MINUTE=20
CLOUD_TOKENS_PER_MINUTE=200000
PROVIDER_IDEMPOTENCY_HEADER=
